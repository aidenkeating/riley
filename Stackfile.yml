mongodb:
  image: mongo
  hostname: mongodb
  expose:
    - "27017"

todo-app:
  image: aidenkeating/todo-node
  hostname: todo-app
  links:
    - mongodb
  environment:
    - MONGODB_PORT=mongodb:27017
  ports:
    - "8081:8080"
